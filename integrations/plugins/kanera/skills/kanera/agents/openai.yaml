interface:
  display_name: "Kanera"
  short_description: "Read and update Kanera cards and project work"
  default_prompt: "Use $kanera to inspect a card by key and update it with my findings."
dependencies:
  tools:
    - type: "mcp"
      value: "kanera"
      description: "Live Kanera project data and actions"
      transport: "streamable_http"
      url: "https://mcp.kanera.app/mcp"
policy:
  allow_implicit_invocation: true
